**ReadME for starting Storage Manager service**

1. After copying the contents of the StorageManager dir and going into the dir, run 'npm install' to get the required packages.

2. Run 'node StorageManager.js' to teh start teh storage amnager service which is ready to receive POST requests with the client details and hte challenge video and places them in the 'registrations' dir.